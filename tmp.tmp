	ifneq ("$(wildcard ${TEMP_DIR}/${BUTANE})","")
		${DOCKER} run --rm \
			--volume ${PWD}:/workdir:Z \
			${YAML_PROCESSOR_IMAGE} \
			--inplace \
			eval 'del(.ignition.config.merge)' ${TEMP_DIR}/${BUTANE}
	endif
